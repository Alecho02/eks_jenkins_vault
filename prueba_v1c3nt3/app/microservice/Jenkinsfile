pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('List files') {
            steps {
                sh 'pwd'
                echo "Contenido del repo:"
                sh 'ls -R'
            }
        }
    }
}
