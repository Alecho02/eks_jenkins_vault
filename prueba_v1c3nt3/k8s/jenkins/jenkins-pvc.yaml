apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-pvc
  namespace: jenkins
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi          # tamaño del EBS para Jenkins
  storageClassName: gp2      # StorageClass que tienes en el cluster
